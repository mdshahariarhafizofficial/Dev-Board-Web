// Show Alert Alert
function updatedAlert() {
    alert('Board updated Successfully')
}

// Text to Number Converter
function textToNumber(id) {
    const innerText = document.getElementById(id).innerText;
    const number = parseInt(innerText);
    return number;
}